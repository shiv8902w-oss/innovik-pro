<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clothing Management - Vyapar Saathi</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-green: #16a34a;
            --primary-green-dark: #15803d;
            --primary-green-light: #22c55e;
            --light-green-bg: #f0fdf4;
            --border-color: #e5e7eb;
            --text-dark: #1f2937;
            --text-medium: #4b5563;
            --text-light: #6b7280;
            --bg-color: #f9fafb;
            --card-bg: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-dark);
        }

        .main-container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Header Section */
        .page-header {
            background: linear-gradient(90deg, var(--primary-green), var(--primary-green-light));
            color: white;
            padding: 2.5rem 2rem;
            border-radius: 0 0 20px 20px;
            margin-bottom: 2rem;
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            text-decoration: none;
            color: white;
            opacity: 0.9;
            margin-bottom: 1.5rem;
            font-weight: 500;
            transition: opacity 0.2s ease;
        }

        .back-link:hover {
            opacity: 1;
        }

        .page-header h1 {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.25rem;
        }

        .page-header p {
            font-size: 1.125rem;
            opacity: 0.9;
        }

        /* Action Bar */
        .action-bar {
            display: flex;
            gap: 1rem;
            margin-bottom: 2.5rem;
        }

        .action-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.6rem;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: all 0.2s ease;
            border: 2px solid transparent;
        }
        
        .action-btn svg {
            width: 20px;
            height: 20px;
        }

        .btn-primary {
            background-color: var(--primary-green);
            color: white;
        }
        .btn-primary:hover {
            background-color: var(--primary-green-dark);
        }

        .btn-secondary {
            background-color: var(--card-bg);
            color: var(--text-medium);
            border: 2px solid var(--border-color);
        }
        .btn-secondary:hover {
            border-color: var(--primary-green);
            color: var(--primary-green);
        }
        
        /* Product List Area */
        .product-list-container {
            background-color: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 12px;
            padding: 4rem 2rem;
        }

        .empty-state {
            text-align: center;
        }

        .empty-state p {
            font-size: 1.125rem;
            color: var(--text-medium);
            font-weight: 500;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .main-container {
                padding: 0 1rem;
            }
            .page-header {
                padding: 2rem 1.5rem;
                border-radius: 0 0 15px 15px;
            }
            .page-header h1 {
                font-size: 2rem;
            }
            .page-header p {
                font-size: 1rem;
            }
            .action-bar {
                flex-direction: column;
                align-items: flex-start;
            }
        }
    </style>
</head>
<body>

    <div class="main-container">
        <!-- Header Section -->
        <header class="page-header">
            <a href="#" class="back-link">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" width="20" height="20">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5" />
                </svg>
                Back to Home
            </a>
            <h1>Clothing Management</h1>
            <p>AI-powered style recommendations & inventory</p>
        </header>

        <!-- Action Buttons -->
        <div class="action-bar">
            <a href="#" class="action-btn btn-primary">
                <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M9.813 15.904L9 18.75l-.813-2.846a4.5 4.5 0 00-3.09-3.09L2.25 12l2.846-.813a4.5 4.5 0 003.09-3.09L9 5.25l.813 2.846a4.5 4.5 0 003.09 3.09L15.75 12l-2.846.813a4.5 4.5 0 00-3.09 3.09zM18.259 8.715L18 9.75l-.259-1.035a3.375 3.375 0 00-2.455-2.456L14.25 6l1.036-.259a3.375 3.375 0 002.455-2.456L18 2.25l.259 1.035a3.375 3.375 0 002.456 2.456L21.75 6l-1.035.259a3.375 3.375 0 00-2.456 2.456zM16.898 20.624L16.5 21.75l-.398-1.126a3.375 3.375 0 00-2.456-2.456L12.75 18l1.126-.398a3.375 3.375 0 002.456-2.456L16.5 14.25l.398 1.126a3.375 3.375 0 002.456 2.456L20.25 18l-1.126.398a3.375 3.375 0 00-2.456 2.456z" />
                </svg>
                AI Style Advisor
            </a>
            <a href="#" class="action-btn btn-secondary">
                <svg xmlns="http://www.w3.org/2000/svg" href="/views/upload" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 4.5v15m7.5-7.5h-15" />
                </svg >
                <div  >Add Product</div>
            </a>
        </div>

        <!-- Product List Area -->
        <main class="product-list-container">
            <div class="empty-state">
                <p>No products yet. Add your first clothing item!</p>
            </div>
        </main>
    </div>

</body>
</html>
